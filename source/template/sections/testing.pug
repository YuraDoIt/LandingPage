p Yes i am
  br
  ul
  each val, index in ['zero', 'one', 'two']
    li= index + ': ' + val

  a(href="http:google.com") link google
  div.link text

  br
  ol 
    li list1 
    li list2

- var i=1

p Minute
select 
  while i<=60
    option=i++

- var h=0
p Hour
select
  while h<=24
   option=h++

ul 
  li
   a(href="//google.com") Google

- var links = ['Home', 'Contacts', 'About']
ul 
  each link in links 
    li: a(href=link)=link

//Mixin
mixin list(items, className, active)
  ul(class=className)
    each item, index in items 
      li(class=className+'__item')
        if index === active
          span(class=className + '__active')=item 
        else 
          a(class=className + '__link')=item 

+list(['first', 'second'], 'list', 0)